#!/bin/sh

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
THEME_DIR=$(dirname "$SCRIPT_DIR")
LOCAL_PHPCBF="$THEME_DIR/vendor/bin/phpcbf"

if [ -x "$LOCAL_PHPCBF" ]; then
	exec "$LOCAL_PHPCBF" "$@"
fi

export PATH="/opt/homebrew/bin:$PATH"

exec /opt/homebrew/bin/phpcbf "$@"
